This is the Student Information System (SIS) project built using C# and SQL.

To run:
1. Restore packages
2. Update connection string in App.config
3. Build and Run Program.cs

Includes: DAO layer, entity models, menu-driven console UI, custom exceptions, and SQL script.